# This repository's content runs on Linux runners (composite action) and via
# sh hooks. Normalizing to LF in the repository stops a Windows clone from
# producing a script the runner can't execute.
* text=auto eol=lf
*.png binary
*.jpg binary
